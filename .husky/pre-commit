# Run gitleaks for secret scanning (install: brew install gitleaks)
if command -v gitleaks &> /dev/null; then
  gitleaks protect --staged --no-banner
fi

# Run lint-staged (ruff on backend, eslint on frontend)
bunx lint-staged
