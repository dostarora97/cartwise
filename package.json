{
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1"
  },
  "lint-staged": {
    "backend/**/*.py": [
      "ruff check --fix",
      "ruff format"
    ],
    "ui/**/*.{ts,tsx}": [
      "eslint --fix"
    ]
  }
}
