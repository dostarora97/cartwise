{
  "name": "cartwise",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "20.5.0",
    "@commitlint/config-conventional": "20.5.0",
    "husky": "9.1.7",
    "lint-staged": "16.4.0"
  },
  "lint-staged": {
    "backend/**/*.py": [
      "ruff check --fix",
      "ruff format"
    ],
    "ui/**/*.{ts,tsx}": [
      "eslint --fix"
    ]
  }
}
